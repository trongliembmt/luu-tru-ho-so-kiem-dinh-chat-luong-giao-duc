@model IEnumerable<Website_KĐCLGD.Models.NHANSU>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Login", "Login")
    </p>
    <table>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TAIKHOAN)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MATKHAU)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MA_NQ)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TAIKHOAN)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MATKHAU)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MA_NQ)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.MA_NS }) |
                @Html.ActionLink("Details", "Details", new { id=item.MA_NS }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.MA_NS })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
